# PostgreSQL + Redis Read-Heavy Warm Scenario
#
# This scenario tests read-heavy workloads with warm Redis cache on PostgreSQL storage.
# Represents typical production read workload with caching.

name: "postgres_redis_read_heavy_warm"
description: "Read-heavy workload with warm Redis cache on PostgreSQL storage (production-like)"

# Backend configuration (required)
storage_mode: postgres
cache_mode: redis

# Workload configuration (required)
load_pattern: read_heavy
cache_state: warm
data_scale: medium
payload_variant: standard
rps_profile: constant

# Load generation (optional with defaults)
duration_seconds: 60
connections: 20
threads: 4
warmup_seconds: 10

# Target endpoints (empty = all)
endpoints:
  - "/tasks"
  - "/tasks/{id}"
  - "/tasks/search"
  - "/projects/{id}/progress"

# Pool configuration
pool_sizes:
  database_pool_size: 10
  redis_pool_size: 20

# Concurrency settings
concurrency:
  worker_threads: 4
  database_pool_size: 10
  redis_pool_size: 20
  max_connections: 50

# Extended metadata for workflow integration
metadata:
  test_type: "general_benchmark"
  endpoint: "mixed"
  payload: "medium"
  cache_strategy: "read-through"
  hit_rate: 90
  fail_injection: 0
  retry: false
  profile: false
  tags:
    - postgres
    - redis
    - read-heavy
    - warm-cache
    - production-like
